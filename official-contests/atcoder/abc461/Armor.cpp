#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int a, d;
    cin >> a >> d;
    if (a > d)
    {
        cout << "No";
    }
    else
    {
        cout << "Yes";
    }

    return 0;
}